# AWSDevops-code
Git  command :
Git login with remote repository local repository 
  >git clone https://github.com/vamsiv7/Devops.git
   >cd Devops
   >git status
   >git add --all
   >git status
   >git commit -m "adding file"
   >git pull

Once git push command is used to upload local repository changes to a remote repository.
   >git push

To crate new branche with git:

   >git checkout -b feature

To sweching the branche:

    >git checkout main
    >git branches

To see the commites :
    >git log

To changes move main branches to sub branches commands:
    
    >git pull
    >git push -u origin feature

    >git checkout -b hotfix
    >git branche feature
    >git checkout feature
    >git push -u origin hotfix
 

  To see the command history 

    >history

